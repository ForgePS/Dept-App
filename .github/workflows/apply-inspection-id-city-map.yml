name: Apply inspection id and city map

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm ci
      - run: node scripts/apply-inspection-id-city-map.cjs
      - run: npm run build
      - name: Commit changes
        run: |
          rm .github/workflows/apply-inspection-id-city-map.yml
          rm scripts/apply-inspection-id-city-map.cjs
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/pages/HydrantTestingPage.jsx .github/workflows/apply-inspection-id-city-map.yml scripts/apply-inspection-id-city-map.cjs
          git commit -m "Show hydrant id on inspection and frame city map"
          git push
