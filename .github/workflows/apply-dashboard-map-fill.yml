name: Apply dashboard map fill

on:
  push:
    paths:
      - '.github/workflows/apply-dashboard-map-fill.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: node scripts/apply-dashboard-map-fill.cjs
      - run: npm run build
      - name: Commit patch
        run: |
          rm .github/workflows/apply-dashboard-map-fill.yml scripts/apply-dashboard-map-fill.cjs
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/pages/HydrantTestingPage.jsx .github/workflows/apply-dashboard-map-fill.yml scripts/apply-dashboard-map-fill.cjs
          git commit -m "Make dashboard map fill available space"
          git push
