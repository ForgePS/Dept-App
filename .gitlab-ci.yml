image: node:20

pages:
  stage: deploy
  script:
    - npm ci
    - npm run build
    - npm install firebase 
    - rm -rf public
    - cp -r dist public
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
